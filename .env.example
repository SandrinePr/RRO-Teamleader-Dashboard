# Frontend: base URL van je C# API (alleen lokaal bij npm run dev).
# Laat leeg of zet NIET op Railway — productie gebruikt same-origin (lege waarde).
VITE_API_URL=http://localhost:5055

# Vite = http://localhost:5173 — zelfde URL als Redirect URI (SPA) in Microsoft Entra.
